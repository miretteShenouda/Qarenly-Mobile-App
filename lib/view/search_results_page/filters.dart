// search_filters.dart

import 'package:flutter/material.dart';

class SearchFiltersPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Filters'),
      ),
      body: Center(
        child: Text('More filters will be added here'),
      ),
    );
  }
}
